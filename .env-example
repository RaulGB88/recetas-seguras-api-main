## Copy this file to .env and fill the values for local development.
## Do NOT commit your .env file.

# Base64-encoded secret used to sign JWTs. Generate a secure random key.
# Example generation (PowerShell):
# $bytes = New-Object byte[] 32; [System.Security.Cryptography.RandomNumberGenerator]::Create().GetBytes($bytes); [Convert]::ToBase64String($bytes)
JWT_SECRET=YOUR_BASE64_SECRET_HERE

# CORS allowed origins (comma-separated)
APP_CORS_ALLOWED_ORIGINS=http://localhost:3000

# Refresh token cleanup interval in milliseconds (default 3600000 = 1h)
APP_TOKEN_CLEANUP_MS=3600000

# Optional overrides
JWT_ACCESS_EXP_MS=3600000
JWT_REFRESH_EXP_MS=2592000000

# Database (optional local overrides)
# SPRING_DATASOURCE_URL=jdbc:mysql://localhost:3306/recetas_seguras?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
# SPRING_DATASOURCE_USERNAME=root
# SPRING_DATASOURCE_PASSWORD=root
